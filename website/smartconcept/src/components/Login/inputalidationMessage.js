import React from "react";
import { HelpBlock } from "react-bootstrap";

const InputalidationMessage = ({ message }) => <HelpBlock style={{ color: 'red' }}>{message}</HelpBlock>

export default InputalidationMessage;